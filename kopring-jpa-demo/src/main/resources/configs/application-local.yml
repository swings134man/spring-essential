spring:
  config:
    activate:
      on-profile: local

  h2:
    console:
      enabled: true

  # JPA Setting
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none #fixme
    defer-datasource-initialization: false #fixme
  sql:
    init:
      mode: never #fixme


logging:
  level:
    root: info